## on ubuntu 9.10

	 sudo apt-get install vncviewer

and then `vncviewer xxx.xxx.xxx.xxx`, the serverside just go system->remote desktop->allow 

NOTE: in later versions of ubuntu the vncviewer name changed, just try

    aptitude search vnc

and one can easily get it.
