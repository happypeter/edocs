

const guidelines = {
  design: {
    /**
     * perfect cube shape, so that I can make shelving with them
     */
  },
}
const dimesions = {
  firewood_container: {
    length: 60,
    height: 30,else
    width: 40
  },
  hardware_container: {
    /**
     * for smaller, but heavy stuff, or anything
     * 20t x 30w x 45l
     */
    
  }
}




const repetitve_cut = () => {
  /**
   * new we need to deal with a lot of long and narrow wood boards,
   * so we need to use miter saw to do a lot repetitive cuts
   * clamp a long board, as a long fence, on which stop blocks clipped
   */

}